# Server for API Days demo

Expose a very simple web API for demonstration purpose for a APIdays talk.

To start it: `npm start`

Use v1.0.0: `git checkout v1.0.0 && npm start`

Use v2.0.0: `git checkout v2.0.0 && npm start`
